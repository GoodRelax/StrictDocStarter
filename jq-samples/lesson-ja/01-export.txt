  絞り込みは JSON を出した「後」に行います。

  StrictDoc 自身にも問い合わせ言語がありますが、 JSON には効きません。
  --filter-nodes は .sdoc と HTML の出力しか絞らず、 --formats=json に付けても
  エラーも警告も出さないまま全件が出ます (0.27.1 で実測)。
  だから「全量を出してから引く」以外の道はありません。
