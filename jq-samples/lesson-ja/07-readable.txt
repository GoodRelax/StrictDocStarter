  index.json の中で日本語は \uXXXX に化けています。
  StrictDoc が json.dumps を既定のまま呼んでいるためで、
  これを変える設定は StrictDoc 側にありません。

  jq で引いている限り目に入りません。 ここまでの出力がその証拠です。
  効くのは JSON ファイルそのものを人や機械へ渡すときだけで、
  その場合は jq を 1 回通せば直り、 しかも小さくなります。

  なおこの書き出しを PowerShell でやってはいけません。
  PowerShell の > は UTF-16 で書くため、 jq が読めないファイルができます。
