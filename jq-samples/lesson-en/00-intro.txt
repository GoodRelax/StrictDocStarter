  Export the whole specification to JSON, then pull just the part you want out
  of it with jq. This walks through that in 7 steps.

  Each step explains itself, waits for Enter, then runs the command it showed.
  Type q at any prompt and press Enter to stop early.
