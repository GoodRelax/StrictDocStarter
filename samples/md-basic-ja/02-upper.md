# 基本 - 上位要求

**Grammar**: basic.sgra \
**UID**: DOC-UPPER \
**Version**: 1.0

本書は **何を作るか**だけを書く。 どう作るかは `03-lower.md` に分けてある。

この段落は要求ではない地の文であり UID を持たない。 **見出しの下に置いた文は、
そのままでは要求の本文と見なされる。** 文書の H1 直下だけは例外で、 地の文として
扱われる。 章の中で地の文を書くときは `**Type**: SECTION` を明示する
(`01-guide-for-human.md` の各章がそうしている)。

付けるかどうかの基準は 1 つだけである — **後から名前で引きたいなら付ける。**

## ファイルの変換

**UID**: SYS-001 \
**STATUS**: Approved

**Statement**: 本ツールは、 利用者が指定した入力ファイルを、 指定された出力形式へ変換すること。

**Rationale**: 本ツールの存在理由そのものである。 これを外すと他の要求が意味を失う。

## 想定外の入力の拒否

**UID**: SYS-002 \
**STATUS**: Approved

**Statement**: 本ツールは、 入力ファイルが指定された形式でない場合、 変換を行わないこと。

## 既存ファイルの保護

**UID**: SYS-003 \
**STATUS**: Reviewed

**Statement**: 本ツールは、 出力先に同名のファイルが既にある場合、 これを上書きしないこと。

**Rationale**: 無警告で既存ファイルを潰すと、 利用者は取り返しのつかない操作に気づけない。 既定は安全側に倒す。
