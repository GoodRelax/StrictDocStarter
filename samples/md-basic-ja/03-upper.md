# 上位要求

**Grammar**: basic.sgra \
**UID**: DOC-UPPER \
**Version**: 1.0

本書は **何を作るか**だけを書く。 どう作るかは `04-lower.md` に分けてある。

この段落は要求ではない地の文であり UID を持たない。 **見出しの下に置いた文は、
StrictDoc はそのままでは要求の本文と見なす。** 文書の H1 直下だけは例外で、
StrictDoc はそこを地の文として扱う。 章の中で地の文を書くときは `**Type**: SECTION` を明示する
(`02-guide-for-human.md` の各章がそうしている)。

付けるかどうかの基準は 1 つだけである — **後から名前で引きたいなら付ける。**

## 要求の書き方 - EARS

**Type**: SECTION

本書と `04-lower.md` の要求は、 すべて **EARS** の 5 つの型のいずれかで書いてある。
EARS は要求文の形を 5 つに絞る書き方であり、 条件の書き忘れと、 1 つの文へ複数の
要求を詰め込むことを防ぐ。

| 型 | 形 |
|---|---|
| 遍在型 | 〈システム〉は、〈応答〉すること。 |
| 事象駆動型 | 〈トリガ〉したとき、〈システム〉は、〈応答〉すること。 |
| 状態駆動型 | 〈状態〉の間、〈システム〉は、〈応答〉すること。 |
| 望ましくない挙動型 | もし〈条件〉ならば、〈システム〉は、〈応答〉すること。 |
| 任意機能型 | 〈機能〉を備える場合、〈システム〉は、〈応答〉すること。 |

**条件は主語より先に書く。** これが EARS の要点である。 主語を先頭に置いて
「本ツールは、 〜の場合、 〜すること。」 と書くと、 読む側は条件に行き着くまで主語を
抱えたままになり、 条件の抜けにも気づけない。 条件を先に出せば、
**その要求がいつ効くのかが文頭で決まる。**

**この一式は EARS 専用のフィールドをあえて持たない。** 型は `**Statement**:` の文
そのものに現れる。 型名を別のフィールドに書くと、 フィールドと本文がずれたときに
どちらを信じるのかを決められなくなる。 代わりに `audit.sh` の `wording candidates`
が、 文そのものの形 (`ears-shape` と `ears-order`) を検査する。

ただし `negative` の行は消さなくてよい。 望ましくない挙動型は 「〜しないこと。」 と
書くのが正しいためである。

## ファイルの変換

**UID**: SYS-001 \
**STATUS**: Approved \
**REVIEW_STATUS**: NoFinding

**Statement**: 本ツールは、 利用者が指定した入力ファイルを、 利用者が指定した出力形式へ変換すること。

**Rationale**: 本ツールの存在理由そのものである。 これを外すと他の要求が意味を失う。

## 想定外の入力の拒否

**UID**: SYS-002 \
**STATUS**: Approved \
**REVIEW_STATUS**: Fixed

**Statement**: もし入力ファイルが利用者の指定した形式でないならば、 本ツールは、 変換を行わないこと。

**REVIEW_COMMENT**: 下位要求 SW-002 が、 変換を拒む条件を「検査する」という手順としてしか書いておらず、 どの入力を拒むのかを文の先頭から読み取れなかった。

**REVIEW_ACTION**: SW-002 を、 条件から始まる形に書き直した。 拒む入力が文の先頭で分かる。

## 既存ファイルの保護

**UID**: SYS-003 \
**STATUS**: Reviewed \
**REVIEW_STATUS**: Open

**Statement**: もし出力先に同名のファイルが既にあるならば、 本ツールは、 そのファイルを上書きしないこと。

**Rationale**: 無警告で既存ファイルを潰すと、 利用者は取り返しのつかない操作に気づけない。 既定は安全側に倒す。

**REVIEW_COMMENT**: 「上書きしない」とだけ書いてあるが、 同名のファイルがあったときに何をするのかが決まっていない。 中断か、 別名か。
